name: LearnUS 일정 자동 수집

on:
  schedule:
    # 매일 오전 7시 KST (= UTC 22:00, 전날)
    - cron: '0 22 * * *'
  # 수동 실행도 가능 (GitHub Actions 탭에서 "Run workflow" 버튼)
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
      - name: 저장소 체크아웃
        uses: actions/checkout@v4
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

      - name: Python 설정
        uses: actions/setup-python@v5
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        with:
          python-version: '3.11'

      - name: 의존성 설치
        run: pip install requests

      - name: Secrets 확인 (디버그)
        env:
          LEARNUS_ID: ${{ secrets.LEARNUS_ID }}
          LEARNUS_PW: ${{ secrets.LEARNUS_PW }}
        run: |
          echo "ID 앞 3자리: ${LEARNUS_ID:0:3}***"
          echo "ID 길이: ${#LEARNUS_ID}"
          echo "PW 길이: ${#LEARNUS_PW}"

      - name: LearnUS 데이터 수집 → GAS 전송
        env:
          LEARNUS_ID: ${{ secrets.LEARNUS_ID }}
          LEARNUS_PW: ${{ secrets.LEARNUS_PW }}
          GAS_URL:    ${{ secrets.GAS_URL }}
        run: python fetch_learnus.py
